{
  "name": "finaudit-server",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python -m pip install -r requirements.txt && python -m uvicorn app.main:app --host 0.0.0.0 --port 3000 --reload",
    "build": "python -m pip install -r requirements.txt",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "clean": "rm -rf __pycache__ venv"
  }
}
